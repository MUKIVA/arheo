[package]
name = "arheo"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = "1.14"

[build-dependencies]
slint-build = "1.14"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
